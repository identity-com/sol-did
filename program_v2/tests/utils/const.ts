import { ExtendedCluster } from '@identity.com/sol-did-client-legacy/dist/lib/constants';

export const INITIAL_ACCOUNT_SIZE = 8 + 60;
export const DEFAULT_SEED_STRING = 'did-account';

export const TEST_CLUSTER: ExtendedCluster = 'localnet';
